# === Core ===
# 'openai' (default) or 'local'
EMBEDDING_PROVIDER=openai

# Required if EMBEDDING_PROVIDER=openai
OPENAI_API_KEY=

# === /ask_pro defaults (optional) ===
# Minimum distinct sources to cite when available
ARP_MIN_DISTINCT_SOURCES=2
# Number of candidate answers for self-consistency voting
ARP_SELF_CONSISTENCY_N=5
