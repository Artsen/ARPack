# === Core ===
# 'openai' (default) or 'local'
EMBEDDING_PROVIDER=openai
# Required if EMBEDDING_PROVIDER=openai
OPENAI_API_KEY=

# === /ask_pro defaults (read by API and MCP) ===
ARP_MIN_DISTINCT_SOURCES=2
ARP_SELF_CONSISTENCY_N=5

# === MCP wrapper for ARPack HTTP API ===
# Base URL of your FastAPI server
ARPACK_URL=http://127.0.0.1:8000
# Optional timeout for MCP -> API calls (seconds)
ARPACK_TIMEOUT_SECONDS=60

# --- API / runtime paths & defaults ---
# Path to FAISS index (used by API)
ARP_INDEX=data/index.faiss
# Sidecar metadata path (defaults to ARP_INDEX + .meta.json)
ARP_META=
# Path to chunks JSONL used by API
ARP_CHUNKS=data/chunks.jsonl
# Default chunks per paper when returning results
ARP_CHUNKS_PER_PAPER=2
# Maximum context chars to assemble for an answer (API safety)
ARP_MAX_CTX_CHARS=12000
# Enable generated-knowledge (GK) expansions in /ask_pro (true/false)
ARP_GK_ENABLED=true

# --- Optional extraction tooling ---
# If you have pandoc installed in a custom path, set it here; otherwise pandoc must be on PATH
ARP_PANDOC_PATH=
